name: Deploy
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Connect to Tailscale
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}       
          username: ${{ secrets.SERVER_USER }} 
          key: ${{ secrets.SSH_PRIVATE_KEY }} 
          script: |
            cd ${{ vars.PROJECT_PATH }}
            
            git pull origin main
            
            /root/.local/bin/uv sync
            sudo systemctl daemon-reload
            sudo systemctl restart dota-live-monitor.service
            sudo systemctl restart dota-dashboard.service
            
            # Check status of both
            systemctl is-active --quiet dota-live-monitor.service && echo "Monitor OK"
            systemctl is-active --quiet dota-dashboard.service && echo "Dashboard OK"